# Example contract: one policy for a simple demo.
# `geval init` uses descriptive policy paths like policies/safety-and-blocking.yaml — list every file here.

name: demo
version: "1.0.0"
policies:
  - path: policy.yaml
